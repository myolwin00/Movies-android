<?xml version="1.0" encoding="utf-8"?>
<transitionSet xmlns:android="http://schemas.android.com/apk/res/android"
    android:transitionOrdering="together"
    android:duration="500">
    <fade>
        <targets>
            <target android:excludeId="@android:id/statusBarBackground" />
            <target android:excludeId="@android:id/navigationBarBackground" />
        </targets>
    </fade>

    <slide android:slideEdge="top">
        <targets>
            <target android:targetId="@id/edt_search" />
            <target android:targetId="@id/appbar" />
        </targets>
    </slide>

    <slide android:slideEdge="left">
        <targets>
            <target android:targetId="@android:id/home" />
            <target android:targetId="@id/layout_movie_detail" />
        </targets>
    </slide>

    <slide android:slideEdge="bottom">
        <targets>
            <target android:targetId="@id/layout_movie_trailers" />
        </targets>
    </slide>

    <slide android:slideEdge="right">
        <targets>
            <target android:targetId="@id/ib_clear" />
            <target android:targetId="@id/layout_movie_desc" />
        </targets>
    </slide>

</transitionSet>